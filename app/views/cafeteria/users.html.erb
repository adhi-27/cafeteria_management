<% if flash[:success] %>
    <p class="Flash-success"><%= flash[:success] %> </p>
<% end %>

<% @users = @users.where.not(name: 'Walk-in customer')%>

<%= render partial: "user-section", locals: {
       user_type: "Customer",
       users: @users.where(role: 'Customer'),
       change: "Make Clerk" }
%>

<%= render partial: "user-section", locals: {
       user_type: "Billing Clerk",
       users: @users.all.where(role: "Billing Clerk"),
       change: "Make Customer" }
%>
